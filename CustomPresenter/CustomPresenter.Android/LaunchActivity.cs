using Android.App;
using Intersoft.Crosslight.Android;

namespace CustomPresenter.Android
{
    [Activity(Label = "CustomPresenter.Android", MainLauncher = true, NoHistory = true, Icon = "@drawable/icon", Theme = "@style/Theme.Splash")]
    public class LaunchActivity : StartActivity
    {
    }
}